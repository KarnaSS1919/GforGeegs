class Solution {
  public:
    bool binarySearch(vector<int>& arr, int k) {
        bool x = true;
        bool y = false;
        int n = arr.size();
        for(int i =0; i<n; i++){
            if(arr[i]== k)
              return x;
            }
         return y; 
        }
        
    
};